<h1>Listing projects</h1>

<hr>

<%= link_to 'New Project', new_project_path, id: "projects_new_link", class: "btn btn-primary", remote: true %>

<br />
<br />

<table id="projects_table_list" class="table table-striped table-condensed table-hover">
  <tr>
    <th>Projects</th>
    <th></th>
    <th></th>
  </tr>

<% @projects.each do |project| %>
  <tr id="<%= project.id %>">
    <%= render partial: "project_table_row", locals: {project: project} %>
  </tr>
<% end %>

</table>

<br />

<hr>
<%= link_to 'New Project remote true', new_project_path,  class: "btn btn-primary", remote: true %>

